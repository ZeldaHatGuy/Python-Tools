The following is a server check in script to allow the user to run commands and output Json to a payload
It is untested.

TODOS:

Add functionality to simply print out a json report instead of send it to an API.